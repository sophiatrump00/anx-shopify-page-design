{{ 'suntneew-pdp.css' | asset_url | stylesheet_tag }}
<script src="{{ 'suntneew-pdp.js' | asset_url }}" defer></script>

{%- assign qr_support_enabled = false -%}
{%- assign qr_support_model = '' -%}
{%- assign qr_support_intro = '' -%}
{%- assign qr_support_install_intro = '' -%}
{%- assign qr_support_fact_one_value = '' -%}
{%- assign qr_support_fact_one_label = '' -%}
{%- assign qr_support_fact_two_value = '' -%}
{%- assign qr_support_fact_two_label = '' -%}
{%- assign qr_support_fact_three_value = '' -%}
{%- assign qr_support_fact_three_label = '' -%}
{%- assign qr_support_fact_four_value = '' -%}
{%- assign qr_support_fact_four_label = '' -%}
{%- case template.suffix -%}
  {%- when 'rv-g24' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'Group 24 RV battery' -%}
    {%- assign qr_support_intro = 'Product support, documents and installation checks for your 12.8V 100Ah Group 24 battery.' -%}
    {%- assign qr_support_install_intro = 'Confirm the tray, terminal orientation and charging setup before securing the battery.' -%}
    {%- assign qr_support_fact_one_value = '12.8V' -%}{%- assign qr_support_fact_one_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_two_value = '100Ah' -%}{%- assign qr_support_fact_two_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_three_value = '1,280Wh' -%}{%- assign qr_support_fact_three_label = 'Rated energy' -%}
    {%- assign qr_support_fact_four_value = 'IP65' -%}{%- assign qr_support_fact_four_label = 'Dust-tight; water jets' -%}
  {%- when 'rv-g31' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'Group 31 RV battery' -%}
    {%- assign qr_support_intro = 'Product support, documents and installation checks for your 12.8V 100Ah Group 31 battery.' -%}
    {%- assign qr_support_install_intro = 'Confirm the tray, terminal orientation and charging setup before securing the battery.' -%}
    {%- assign qr_support_fact_one_value = '12.8V' -%}{%- assign qr_support_fact_one_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_two_value = '100Ah' -%}{%- assign qr_support_fact_two_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_three_value = '1,280Wh' -%}{%- assign qr_support_fact_three_label = 'Rated energy' -%}
    {%- assign qr_support_fact_four_value = 'IP65' -%}{%- assign qr_support_fact_four_label = 'Dust-tight; water jets' -%}
  {%- when 'rv-230ah' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = '230Ah RV battery' -%}
    {%- assign qr_support_intro = 'Product support, documents and installation checks for your 12.8V 230Ah RV battery.' -%}
    {%- assign qr_support_install_intro = 'Confirm the physical fit, M8 terminal access and LiFePO4 charging limits before installation.' -%}
    {%- assign qr_support_fact_one_value = '12.8V' -%}{%- assign qr_support_fact_one_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_two_value = '230Ah' -%}{%- assign qr_support_fact_two_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_three_value = '2,944Wh' -%}{%- assign qr_support_fact_three_label = 'Rated energy' -%}
    {%- assign qr_support_fact_four_value = '200A' -%}{%- assign qr_support_fact_four_label = 'Max continuous discharge' -%}
  {%- when 'rv-314ah' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = '314Ah RV battery' -%}
    {%- assign qr_support_intro = 'Product support, documents and installation checks for your 12.8V 314Ah RV battery.' -%}
    {%- assign qr_support_install_intro = 'Confirm the physical fit, M8 terminal access and LiFePO4 charging limits before installation.' -%}
    {%- assign qr_support_fact_one_value = '12.8V' -%}{%- assign qr_support_fact_one_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_two_value = '314Ah' -%}{%- assign qr_support_fact_two_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_three_value = '4,019Wh' -%}{%- assign qr_support_fact_three_label = 'Rated energy' -%}
    {%- assign qr_support_fact_four_value = '157A' -%}{%- assign qr_support_fact_four_label = 'Max continuous operation' -%}
  {%- when 'a20' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'A20 jump starter' -%}
    {%- assign qr_support_intro = 'Choose the guide that matches the capacity printed on your A20 product label.' -%}
    {%- assign qr_support_install_intro = 'Charge the unit, identify the 12V vehicle battery terminals and follow the smart-clamp sequence in the matching guide.' -%}
    {%- assign qr_support_fact_one_value = '12V' -%}{%- assign qr_support_fact_one_label = 'Vehicle system' -%}
    {%- assign qr_support_fact_two_value = '8–16Ah' -%}{%- assign qr_support_fact_two_label = 'Capacity options' -%}
    {%- assign qr_support_fact_three_value = 'USB-C' -%}{%- assign qr_support_fact_three_label = 'Charging input' -%}
    {%- assign qr_support_fact_four_value = '3' -%}{%- assign qr_support_fact_four_label = 'Product guides' -%}
  {%- when 'a3' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'A3 jump starter' -%}
    {%- assign qr_support_intro = 'Use the A3 product guide for the documented starting sequence, charging and safety limits.' -%}
    {%- assign qr_support_install_intro = 'Charge the unit, confirm a compatible 12V vehicle battery and connect the smart clamps in the documented order.' -%}
    {%- assign qr_support_fact_one_value = '12V' -%}{%- assign qr_support_fact_one_label = 'Vehicle system' -%}
    {%- assign qr_support_fact_two_value = '16,000mAh' -%}{%- assign qr_support_fact_two_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_three_value = 'USB-C' -%}{%- assign qr_support_fact_three_label = 'Charging input' -%}
    {%- assign qr_support_fact_four_value = 'Smart clamp' -%}{%- assign qr_support_fact_four_label = 'Connection protection' -%}
  {%- when 'kb700' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'KB700 jump starter' -%}
    {%- assign qr_support_intro = 'Use the KB700 guide for vehicle starting, air-duster operation, charging and smart-clamp safety.' -%}
    {%- assign qr_support_install_intro = 'Charge the unit, check the smart-clamp indicators and use the correct operating mode before starting or cleaning.' -%}
    {%- assign qr_support_fact_one_value = '7,200mAh' -%}{%- assign qr_support_fact_one_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_two_value = '1,000A' -%}{%- assign qr_support_fact_two_label = 'Peak current' -%}
    {%- assign qr_support_fact_three_value = '500A' -%}{%- assign qr_support_fact_three_label = 'Starting current' -%}
    {%- assign qr_support_fact_four_value = '105,000 RPM' -%}{%- assign qr_support_fact_four_label = 'Maximum fan speed' -%}
  {%- when 'u23' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'U23 jump starter' -%}
    {%- assign qr_support_intro = 'Use the U23 guide for the documented vehicle-starting sequence, charging and clamp safety checks.' -%}
    {%- assign qr_support_install_intro = 'Charge the unit, turn off vehicle loads and follow the smart-clamp indicator sequence before attempting a start.' -%}
    {%- assign qr_support_fact_one_value = '8,000mAh' -%}{%- assign qr_support_fact_one_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_two_value = '1,500A' -%}{%- assign qr_support_fact_two_label = 'Peak current' -%}
    {%- assign qr_support_fact_three_value = '700A' -%}{%- assign qr_support_fact_three_label = 'Starting current' -%}
    {%- assign qr_support_fact_four_value = '29.6Wh' -%}{%- assign qr_support_fact_four_label = 'Rated energy' -%}
  {%- when 'u32' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'U32 jump starter' -%}
    {%- assign qr_support_intro = 'Use the U32 guide for the documented vehicle-starting sequence, charging and clamp safety checks.' -%}
    {%- assign qr_support_install_intro = 'Charge the unit, turn off vehicle loads and follow the smart-clamp indicator sequence before attempting a start.' -%}
    {%- assign qr_support_fact_one_value = '10,000mAh' -%}{%- assign qr_support_fact_one_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_two_value = '2,000A' -%}{%- assign qr_support_fact_two_label = 'Peak current' -%}
    {%- assign qr_support_fact_three_value = '1,000A' -%}{%- assign qr_support_fact_three_label = 'Starting current' -%}
    {%- assign qr_support_fact_four_value = '37Wh' -%}{%- assign qr_support_fact_four_label = 'Rated energy' -%}
  {%- when 'home-vh' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'VH home energy storage' -%}
    {%- assign qr_support_intro = 'Review the current product specification before system planning or installer coordination.' -%}
    {%- assign qr_support_install_intro = 'Confirm the selected VH configuration, inverter compatibility, site plan and approved installation documentation before commissioning.' -%}
    {%- assign qr_support_fact_one_value = '10.24 / 15.36kWh' -%}{%- assign qr_support_fact_one_label = 'System energy' -%}
    {%- assign qr_support_fact_two_value = '102.4 / 153.6V' -%}{%- assign qr_support_fact_two_label = 'System voltage' -%}
    {%- assign qr_support_fact_three_value = 'IP65' -%}{%- assign qr_support_fact_three_label = 'Enclosure rating' -%}
    {%- assign qr_support_fact_four_value = 'CAN / RS485' -%}{%- assign qr_support_fact_four_label = 'Communication' -%}
  {%- when 'home-wl5a' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'WL5A home battery' -%}
    {%- assign qr_support_intro = 'Review the current product specification before system planning or installer coordination.' -%}
    {%- assign qr_support_install_intro = 'Confirm the system design, inverter compatibility, protection devices and approved installation documentation before commissioning.' -%}
    {%- assign qr_support_fact_one_value = '5.12kWh' -%}{%- assign qr_support_fact_one_label = 'Rated energy' -%}
    {%- assign qr_support_fact_two_value = '51.2V' -%}{%- assign qr_support_fact_two_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_three_value = '100Ah' -%}{%- assign qr_support_fact_three_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_four_value = 'IP54' -%}{%- assign qr_support_fact_four_label = 'Enclosure rating' -%}
  {%- when 'home-wl10b' -%}
    {%- assign qr_support_enabled = true -%}
    {%- assign qr_support_model = 'WL10B home battery' -%}
    {%- assign qr_support_intro = 'Review the current product specification before system planning or installer coordination.' -%}
    {%- assign qr_support_install_intro = 'Confirm the system design, inverter compatibility, protection devices and approved installation documentation before commissioning.' -%}
    {%- assign qr_support_fact_one_value = '10.24kWh' -%}{%- assign qr_support_fact_one_label = 'Rated energy' -%}
    {%- assign qr_support_fact_two_value = '51.2V' -%}{%- assign qr_support_fact_two_label = 'Rated voltage' -%}
    {%- assign qr_support_fact_three_value = '200Ah' -%}{%- assign qr_support_fact_three_label = 'Rated capacity' -%}
    {%- assign qr_support_fact_four_value = 'IP54' -%}{%- assign qr_support_fact_four_label = 'Enclosure rating' -%}
{%- endcase -%}

<section class="sn-pdp sn-pdp-details sn-pdp__section" data-pdp-panel-host>
  <div class="sn-pdp__shell">
    <div id="{{ section.settings.anchor_id | handleize }}" class="sn-pdp-details__panel" role="tabpanel" data-pdp-panel="{{ section.settings.anchor_id | handleize }}">
      {%- if section.settings.eyebrow != blank -%}<p class="sn-pdp__eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
      <h2 class="sn-pdp__heading">{{ section.settings.heading }}</h2>
      {%- if section.settings.lede != blank -%}<div class="sn-pdp__lede">{{ section.settings.lede }}</div>{%- endif -%}

      <div class="sn-pdp-spec-groups">
        {%- assign group_keys = 'primary,system,installation' | split: ',' -%}
        {%- for group_key in group_keys -%}
          {%- assign group_count = 0 -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'spec' and block.settings['group'] == group_key -%}
              {%- assign group_count = group_count | plus: 1 -%}
            {%- endif -%}
          {%- endfor -%}

          {%- if group_count > 0 -%}
            {%- case group_key -%}
              {%- when 'system' -%}{%- assign group_heading = section.settings.system_group_heading -%}
              {%- when 'installation' -%}{%- assign group_heading = section.settings.installation_group_heading -%}
              {%- else -%}{%- assign group_heading = section.settings.primary_group_heading -%}
            {%- endcase -%}

            <details class="sn-pdp-spec-group"{% if forloop.first %} open{% endif %}>
              <summary>
                <span>{{ group_heading }}</span>
                <span class="sn-pdp-spec-group__count">View details</span>
              </summary>
              <dl class="sn-pdp-specs">
                {%- for block in section.blocks -%}
                  {%- if block.type == 'spec' and block.settings['group'] == group_key -%}
                    <div class="sn-pdp-specs__row" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>
                      <dt>{{ block.settings.label }}</dt>
                      <dd>{{ block.settings.value }}</dd>
                    </div>
                  {%- endif -%}
                {%- endfor -%}
              </dl>
            </details>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>

    {%- assign included_blocks = section.blocks | where: 'type', 'included' -%}
    {%- if included_blocks.size > 0 -%}
      <div id="in-the-box" class="sn-pdp-details__panel" role="tabpanel" data-pdp-panel="in-the-box">
        <p class="sn-pdp__eyebrow">{{ section.settings.included_eyebrow }}</p>
        <h2 class="sn-pdp__heading">{{ section.settings.included_heading }}</h2>
        <ul class="sn-pdp-list sn-pdp-list--cards">
          {%- for block in included_blocks -%}
            <li {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>{{ block.settings.item }}</li>
          {%- endfor -%}
        </ul>
      </div>
    {%- endif -%}

    {%- assign download_blocks = section.blocks | where: 'type', 'download' -%}
    {%- assign manual_count = 0 -%}
    {%- assign compliance_count = 0 -%}
    {%- assign qr_primary_document_url = '' -%}
    {%- for block in download_blocks -%}
      {%- assign document_group = block.settings.document_group | default: 'manuals' -%}
      {%- if block.settings.document_group == blank -%}
        {%- assign label_lower = block.settings.label | downcase -%}
        {%- if label_lower contains 'fcc' or label_lower contains 'emc' or label_lower contains 'rohs' or label_lower contains 'ip65' or label_lower contains 'ul ' or label_lower contains 'un38' or label_lower contains 'sds' or label_lower contains 'msds' or label_lower contains 'tdg' or label_lower contains 'dgm' or label_lower contains 'certificate' or label_lower contains 'report' or label_lower contains 'grant' -%}
          {%- assign document_group = 'compliance' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if document_group == 'compliance' -%}
        {%- assign compliance_count = compliance_count | plus: 1 -%}
      {%- else -%}
        {%- assign manual_count = manual_count | plus: 1 -%}
        {%- if qr_primary_document_url == blank -%}
          {%- if block.settings.link != blank -%}
            {%- assign qr_primary_document_url = block.settings.link -%}
          {%- elsif block.settings.asset_name != blank -%}
            {%- assign qr_primary_document_url = block.settings.asset_name | asset_url -%}
          {%- endif -%}
        {%- endif -%}
      {%- endif -%}
    {%- endfor -%}

    {%- if download_blocks.size > 0 -%}
      <div id="manuals" class="sn-pdp-details__panel" role="tabpanel" data-pdp-panel="manuals"{% if qr_support_enabled %} data-qr-mobile="{{ template.suffix }}"{% endif %}>
        <p class="sn-pdp__eyebrow">{{ section.settings.download_eyebrow }}</p>
        <h2 class="sn-pdp__heading">{{ section.settings.download_heading }}</h2>

        {%- if qr_support_enabled -%}
          <div class="sn-pdp-qr-mobile" aria-label="{{ qr_support_model | escape }} support">
            <div class="sn-pdp-qr-mobile__intro">
              <div class="sn-pdp-qr-mobile__image">
                {%- if product.featured_image -%}
                  <img src="{{ product.featured_image | image_url: width: 720 }}" alt="{{ qr_support_model | escape }}" loading="eager" width="720" height="720">
                {%- endif -%}
              </div>
              <div class="sn-pdp-qr-mobile__intro-copy">
                <p class="sn-pdp-qr-mobile__eyebrow">{{ qr_support_model | upcase }}</p>
                <h3>Product support for your {{ qr_support_model }}.</h3>
                <p>{{ qr_support_intro }}</p>
              </div>
            </div>

            <div class="sn-pdp-qr-mobile__actions" aria-label="{{ qr_support_model | escape }} support actions">
              {%- if manual_count > 1 -%}
                <a class="sn-pdp-qr-mobile__action sn-pdp-qr-mobile__action--primary" href="#qr-product-documents">
                  <span>Choose your product guide</span>
                  <small>Match the guide to your capacity or model</small>
                  <span aria-hidden="true">↓</span>
                </a>
                {%- elsif qr_primary_document_url != blank -%}
                <a class="sn-pdp-qr-mobile__action sn-pdp-qr-mobile__action--primary" href="{{ qr_primary_document_url }}" target="_blank" rel="noopener">
                  <span>{% if template.suffix contains 'home-' %}View product specification{% else %}Download user manual{% endif %}</span>
                  <small>{% if template.suffix contains 'home-' %}Current product data{% else %}Open the model-matched guide{% endif %}</small>
                  <span aria-hidden="true">↗</span>
                </a>
              {%- endif -%}
            </div>

            <div class="sn-pdp-qr-mobile__facts" aria-label="{{ qr_support_model | escape }} key specifications">
              <div><strong>{{ qr_support_fact_one_value }}</strong><span>{{ qr_support_fact_one_label }}</span></div>
              <div><strong>{{ qr_support_fact_two_value }}</strong><span>{{ qr_support_fact_two_label }}</span></div>
              <div><strong>{{ qr_support_fact_three_value }}</strong><span>{{ qr_support_fact_three_label }}</span></div>
              <div><strong>{{ qr_support_fact_four_value }}</strong><span>{{ qr_support_fact_four_label }}</span></div>
            </div>

            <details class="sn-pdp-qr-mobile__details">
              <summary><span>Before installation</span><span aria-hidden="true">＋</span></summary>
              <ul>
                <li>{{ qr_support_install_intro }}</li>
                <li>Use the product document as the source of truth for wiring, operating limits and the complete safety sequence.</li>
                <li>Pause and contact support if the product label, equipment or installation differs from the documented configuration.</li>
              </ul>
            </details>

            {%- if manual_count > 1 -%}
              <details id="qr-product-documents" class="sn-pdp-qr-mobile__details sn-pdp-qr-mobile__details--documents">
                <summary><span>Product guides</span><span aria-hidden="true">＋</span></summary>
                <div class="sn-pdp-qr-mobile__document-list">
                  {%- for block in download_blocks -%}
                    {%- assign document_group = block.settings.document_group | default: 'manuals' -%}
                    {%- if document_group != 'compliance' -%}
                      {%- if block.settings.link != blank -%}
                        <a href="{{ block.settings.link }}" target="_blank" rel="noopener" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %}>{{ block.settings.label }} <span aria-hidden="true">↗</span></a>
                      {%- elsif block.settings.asset_name != blank -%}
                        <a href="{{ block.settings.asset_name | asset_url }}" target="_blank" rel="noopener" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %}>{{ block.settings.label }} <span aria-hidden="true">↗</span></a>
                      {%- endif -%}
                    {%- endif -%}
                  {%- endfor -%}
                </div>
              </details>
            {%- endif -%}

            {%- if compliance_count > 0 -%}
              <details class="sn-pdp-qr-mobile__details sn-pdp-qr-mobile__details--documents">
                <summary><span>Documents & compliance</span><span aria-hidden="true">＋</span></summary>
                <div class="sn-pdp-qr-mobile__chips" aria-label="{{ qr_support_model | escape }} document topics">
                  {%- assign qr_standards_seen = '|' -%}
                  {%- for block in download_blocks -%}
                    {%- if block.settings.document_group == 'compliance' -%}
                      {%- assign qr_standard = block.settings.standard | default: block.settings.label -%}
                      {%- assign qr_standard_lower = qr_standard | downcase -%}
                      {%- assign qr_standard_token = '|' | append: qr_standard_lower | append: '|' -%}
                      {%- unless qr_standards_seen contains qr_standard_token -%}
                        <span>{{ qr_standard }}</span>
                        {%- assign qr_standards_seen = qr_standards_seen | append: qr_standard_lower | append: '|' -%}
                      {%- endunless -%}
                    {%- endif -%}
                  {%- endfor -%}
                </div>
                <p>Use only documents that match the product label. Public technical documents are available from the product page or technology center where shown.</p>
              </details>
            {%- endif -%}

            <a class="sn-pdp-qr-mobile__product-link" href="/pages/contact-us">Need help with {{ qr_support_model }}? <span>Contact support <span aria-hidden="true">→</span></span></a>
          </div>
        {%- endif -%}

        {%- if manual_count > 0 -%}
          <section class="sn-pdp-doc-group" aria-labelledby="{{ section.id }}-manuals-heading">
            <h3 id="{{ section.id }}-manuals-heading" class="sn-pdp-doc-group__heading">{{ section.settings.manuals_group_heading }}</h3>
            <div class="sn-pdp-downloads">
              {%- for block in download_blocks -%}
                {%- assign document_group = block.settings.document_group | default: 'manuals' -%}
                {%- if block.settings.document_group == blank -%}
                  {%- assign label_lower = block.settings.label | downcase -%}
                  {%- if label_lower contains 'fcc' or label_lower contains 'emc' or label_lower contains 'rohs' or label_lower contains 'ip65' or label_lower contains 'ul ' or label_lower contains 'un38' or label_lower contains 'sds' or label_lower contains 'msds' or label_lower contains 'tdg' or label_lower contains 'dgm' or label_lower contains 'certificate' or label_lower contains 'report' or label_lower contains 'grant' -%}
                    {%- assign document_group = 'compliance' -%}
                  {%- endif -%}
                {%- endif -%}
                {%- if document_group != 'compliance' -%}
                  {%- if block.settings.link != blank -%}
                    <a class="sn-pdp-download" href="{{ block.settings.link }}" target="_blank" rel="noopener" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>{{ block.settings.label }}</a>
                  {%- elsif block.settings.asset_name != blank -%}
                    <a class="sn-pdp-download" href="{{ block.settings.asset_name | asset_url }}" target="_blank" rel="noopener" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>{{ block.settings.label }}</a>
                  {%- else -%}
                    <span class="sn-pdp-download" aria-disabled="true" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>{{ block.settings.label }}</span>
                  {%- endif -%}
                {%- endif -%}
              {%- endfor -%}
            </div>
          </section>
        {%- endif -%}

        {%- if compliance_count > 0 -%}
          {%- liquid
            assign compliance_library_anchor = ''
            case template.suffix
              when 'a20'
                assign compliance_library_anchor = 'jump-a20'
              when 'a3'
                assign compliance_library_anchor = 'jump-a3'
              when 'kb700'
                assign compliance_library_anchor = 'jump-kb700'
              when 'u23'
                assign compliance_library_anchor = 'jump-u23'
              when 'u32'
                assign compliance_library_anchor = 'jump-u32'
              when 'rv-g24', 'rv-g31'
                assign compliance_library_anchor = 'rv-g24-g31'
              when 'rv-230ah', 'rv-314ah'
                assign compliance_library_anchor = 'rv-230-314'
            endcase

            assign compliance_library_url = pages['technology'].url | default: '/pages/technology'
            if compliance_library_anchor != blank
              assign compliance_library_url = compliance_library_url | append: '#' | append: compliance_library_anchor
            endif
          -%}
          <section class="sn-pdp-doc-group" aria-labelledby="{{ section.id }}-compliance-heading">
            <h3 id="{{ section.id }}-compliance-heading" class="sn-pdp-doc-group__heading">{{ section.settings.compliance_group_heading }}</h3>
            {%- if template.suffix == 'rv-230ah' -%}
              {%- if section.settings.compliance_note != blank -%}<p class="sn-pdp-doc-group__note">{{ section.settings.compliance_note | escape }}</p>{%- endif -%}
              <div class="sn-pdp-compliance-docs" aria-label="{{ section.settings.compliance_group_heading | escape }}">
                {%- for block in download_blocks -%}
                  {%- assign document_group = block.settings.document_group | default: 'manuals' -%}
                  {%- if block.settings.document_group == blank -%}
                    {%- assign label_lower = block.settings.label | downcase -%}
                    {%- if label_lower contains 'fcc' or label_lower contains 'emc' or label_lower contains 'rohs' or label_lower contains 'ip65' or label_lower contains 'ul ' or label_lower contains 'un38' or label_lower contains 'sds' or label_lower contains 'msds' or label_lower contains 'tdg' or label_lower contains 'dgm' or label_lower contains 'certificate' or label_lower contains 'report' or label_lower contains 'grant' -%}
                      {%- assign document_group = 'compliance' -%}
                    {%- endif -%}
                  {%- endif -%}
                  {%- if document_group == 'compliance' -%}
                    {%- assign compliance_standard = block.settings.standard | default: block.settings.label -%}
                    {%- assign compliance_document_url = '' -%}
                    {%- if block.settings.link != blank -%}
                      {%- assign compliance_document_url = block.settings.link -%}
                    {%- elsif block.settings.asset_name != blank and block.settings.public_copy_ready -%}
                      {%- assign compliance_document_url = block.settings.asset_name | asset_url -%}
                    {%- endif -%}
                    {%- if block.settings.public_copy_ready and compliance_document_url != blank -%}
                      <a class="sn-pdp-compliance-doc" href="{{ compliance_document_url }}" target="_blank" rel="noopener" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>
                        <strong>{{ compliance_standard }}</strong>
                        <span>{{ block.settings.label }}</span>
                        <small>{{ section.settings.compliance_link_label }}</small>
                      </a>
                    {%- else -%}
                      <span class="sn-pdp-compliance-doc sn-pdp-compliance-doc--pending" aria-disabled="true" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>
                        <strong>{{ compliance_standard }}</strong>
                        <span>{{ block.settings.label }}</span>
                        <small>{{ section.settings.compliance_pending_label }}</small>
                      </span>
                    {%- endif -%}
                  {%- endif -%}
                {%- endfor -%}
              </div>
              <a class="sn-pdp-compliance-library-link" href="{{ compliance_library_url }}">
                <span>{{ section.settings.compliance_library_label }}</span>
                <span aria-hidden="true">→</span>
              </a>
            {%- else -%}
              <div class="sn-pdp-compliance-overview">
                <div class="sn-pdp-compliance-badges" aria-label="{{ section.settings.compliance_group_heading | escape }}">
                  {%- assign compliance_standards_seen = '|' -%}
                {%- for block in download_blocks -%}
                  {%- assign document_group = block.settings.document_group | default: 'manuals' -%}
                  {%- if block.settings.document_group == blank -%}
                    {%- assign label_lower = block.settings.label | downcase -%}
                    {%- if label_lower contains 'fcc' or label_lower contains 'emc' or label_lower contains 'rohs' or label_lower contains 'ip65' or label_lower contains 'ul ' or label_lower contains 'un38' or label_lower contains 'sds' or label_lower contains 'msds' or label_lower contains 'tdg' or label_lower contains 'dgm' or label_lower contains 'certificate' or label_lower contains 'report' or label_lower contains 'grant' -%}
                      {%- assign document_group = 'compliance' -%}
                    {%- endif -%}
                  {%- endif -%}
                  {%- if document_group == 'compliance' -%}
                    {%- assign compliance_standard = block.settings.standard | default: block.settings.label -%}
                    {%- assign compliance_standard_lower = compliance_standard | downcase -%}
                    {%- assign compliance_standard_token = '|' | append: compliance_standard_lower | append: '|' -%}
                    {%- unless compliance_standards_seen contains compliance_standard_token -%}
                      <a class="sn-pdp-compliance-badge{% if compliance_standard_lower contains 'ul 2743' or compliance_standard_lower contains 'cell safety' %} sn-pdp-compliance-badge--ul{% endif %}" href="{{ compliance_library_url }}" {% if block.settings.variant_match != blank %}data-variant-match="{{ block.settings.variant_match | escape }}"{% endif %} {{ block.shopify_attributes }}>{{ compliance_standard }}</a>
                      {%- assign compliance_standards_seen = compliance_standards_seen | append: compliance_standard_lower | append: '|' -%}
                    {%- endunless -%}
                  {%- endif -%}
                {%- endfor -%}
                </div>
                <a class="sn-pdp-compliance-library-link" href="{{ compliance_library_url }}">
                  <span>{{ section.settings.compliance_library_label }}</span>
                  <span aria-hidden="true">→</span>
                </a>
              </div>
            {%- endif -%}
          </section>
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name":"SuntNeew PDP details",
  "settings":[
    {"type":"text","id":"anchor_id","label":"Anchor ID","default":"specifications"},
    {"type":"text","id":"eyebrow","label":"Eyebrow","default":"Product details"},
    {"type":"text","id":"heading","label":"Heading","default":"Specifications at a glance"},
    {"type":"richtext","id":"lede","label":"Introduction","default":"<p>Choose a category to inspect the technical details.</p>"},
    {"type":"text","id":"primary_group_heading","label":"Primary specs group","default":"Energy & performance"},
    {"type":"text","id":"system_group_heading","label":"System specs group","default":"Electrical & system"},
    {"type":"text","id":"installation_group_heading","label":"Installation specs group","default":"Installation & environment"},
    {"type":"text","id":"included_eyebrow","label":"Included eyebrow","default":"What is included"},
    {"type":"text","id":"included_heading","label":"Included heading","default":"In the box"},
    {"type":"text","id":"download_eyebrow","label":"Downloads eyebrow","default":"Reference material"},
    {"type":"text","id":"download_heading","label":"Downloads heading","default":"Manuals & Documents"},
    {"type":"text","id":"manuals_group_heading","label":"Manuals group heading","default":"User Manuals"},
    {"type":"text","id":"compliance_group_heading","label":"Compliance group heading","default":"Safety & Compliance"},
    {"type":"text","id":"compliance_library_label","label":"Compliance library link label","default":"View public documents"},
    {"type":"textarea","id":"compliance_note","label":"Compliance note","default":"Only model-matched public copies should be uploaded here. Check the model, report number and scope before relying on a document."},
    {"type":"text","id":"compliance_scope_label","label":"Compliance scope label","default":"View scope and document status"},
    {"type":"text","id":"compliance_link_label","label":"Compliance link label","default":"View public copy"},
    {"type":"text","id":"compliance_pending_label","label":"Pending compliance label","default":"Public copy pending"},
    {"type":"text","id":"compliance_request_label","label":"Compliance request label","default":"Request a model-matched copy"},
    {"type":"url","id":"compliance_request_url","label":"Compliance request URL"}
  ],
  "blocks":[
    {"type":"spec","name":"Specification","settings":[
      {"type":"text","id":"label","label":"Label","default":"Capacity"},
      {"type":"text","id":"value","label":"Value","default":"8,000mAh"},
      {"type":"select","id":"group","label":"Specification group","default":"primary","options":[{"value":"primary","label":"Energy & performance"},{"value":"system","label":"Electrical & system"},{"value":"installation","label":"Installation & environment"}]},
      {"type":"text","id":"variant_match","label":"Only show for variant","info":"Optional capacity, color, title or SKU match."}
    ]},
    {"type":"included","name":"Included item","settings":[
      {"type":"text","id":"item","label":"Item","default":"Product unit"},
      {"type":"text","id":"variant_match","label":"Only show for variant"}
    ]},
    {"type":"download","name":"Download","settings":[
      {"type":"text","id":"label","label":"Label","default":"User manual — pending upload"},
      {"type":"url","id":"link","label":"File or page link"},
      {"type":"text","id":"asset_name","label":"Bundled asset filename","info":"Optional theme asset, for example suntneew-wl5a-spec-v2.jpg."},
      {"type":"text","id":"variant_match","label":"Only show for variant"},
      {"type":"select","id":"document_group","label":"Document group","default":"manuals","options":[{"value":"manuals","label":"User manuals"},{"value":"compliance","label":"Safety & compliance"}]},
      {"type":"text","id":"standard","label":"Standard or badge text","info":"Optional. Used on safety and compliance cards."},
      {"type":"textarea","id":"description","label":"Scope or description","info":"Optional. Explain the model, report scope or document purpose."},
      {"type":"checkbox","id":"public_copy_ready","label":"Public copy is redacted and approved","default":false}
    ]}
  ],
  "max_blocks": 40,
  "presets":[{"name":"SuntNeew PDP details","blocks":[{"type":"spec"},{"type":"spec"},{"type":"included"},{"type":"download"}]}]
}
{% endschema %}
